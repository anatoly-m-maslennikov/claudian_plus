# Disabled GitHub Actions workflows

All repository workflows are temporarily disabled.

GitHub discovers workflow definitions only under `.github/workflows/`. To restore a workflow, move its YAML file from this directory back into `.github/workflows/` and commit the change.
